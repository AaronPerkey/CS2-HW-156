package com.fmt;import java.time.LocalDate;

/**
 * Models how a purchase of equipment works.
 * @author aaron and emma
 *
 */

public class Purchase extends Equipment{
	
	private final String purchase;
	

	public Purchase(String itemsQRCode, String typeOfSale, String name, String model, String purchase) {
		this.purchase = "";
		super(itemsQRCode, typeOfSale, name, model);
	}
	public String getPurchase() {
	double subtotal = 0.00;
	double tax = 0.00;
	double total = 0.00;
	if(getTypeOfSale().equals("P")) {
		total = ;
		return purchase;
	}
	}
}
